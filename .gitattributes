# Normalize line endings for cross-platform use
* text=auto

# bash scripts must stay LF (CRLF breaks them on Linux/macOS)
*.sh text eol=lf

# Windows scripts
*.bat text eol=crlf
*.ps1 text eol=crlf

# Binaries
*.png binary
*.db binary
